[build-system]
requires = [
    "setuptools >= 46.4.0",
]
build-backend = "setuptools.build_meta"

[tool.portray]
modules = ["aiopytesseract"]

[tool.portray.mkdocs.theme]
name = "material"
palette = {primary = "gray", accent = "gray"}
